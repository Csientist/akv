name: akv
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  appwrite: ^21.0.0
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.5
  sqlite3_flutter_libs: ^0.5.0   # ← ADD this
  sqlite3: ^2.4.0                # ← ADD this (the missing transitive dep)
  path: ^1.9.0
  connectivity_plus: ^6.0.0
  uuid: ^4.0.0
  workmanager: ^0.5.2
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  logger: ^2.6.2
  flutter_image_compress: ^2.2.0
  image_picker: ^1.1.2
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  change_app_package_name: ^1.5.0

flutter:
  uses-material-design: true

dependency_overrides:
  flutter_web_auth_2: ^4.1.0
  workmanager: ^0.6.0